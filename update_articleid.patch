--- apps/web/src/routes/(public)/blog/$articleId.tsx
+++ apps/web/src/routes/(public)/blog/$articleId.tsx
@@ -25,6 +25,7 @@
         slug: data?.slug,
         createdAt: data?.createdAt,
         updatedAt: data?.updatedAt,
+        relatedArticles: data?.relatedArticles,
       };
     } catch (error) {
       if (
@@ -250,6 +251,20 @@
             <ArticleComment articleId={article.id} articleSlug={article.slug} />
           </m.div>
         </div>
+
+        {/* Related Articles section */}
+        {article.relatedArticles && article.relatedArticles.length > 0 && (
+          <m.div
+            animate={{ opacity: 1, y: 0 }}
+            className="mt-12 border-border/50 border-t pt-8"
+            initial={false}
+            transition={{ duration: 0.5, delay: 0.7 }}
+          >
+            <h2 className="mb-6 font-heading text-2xl font-bold tracking-tight">Related Posts</h2>
+            <div className="grid gap-6 sm:grid-cols-2 md:grid-cols-3">
+              {article.relatedArticles.map((relatedArticle: any) => (
+                <ArticleCard key={relatedArticle.slug} article={relatedArticle as any} />
+              ))}
+            </div>
+          </m.div>
+        )}

         {/* Table of contents - enhanced sticky sidebar */}
