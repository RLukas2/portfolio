{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".output/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "inputs": ["**/*.{ts,tsx,js,jsx,json,css}"]
    },
    "format": {
      "inputs": ["**/*.{ts,tsx,js,jsx,json,css,md}"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    }
  }
}
